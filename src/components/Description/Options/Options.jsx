const Options = ({ good, neutral, bad }) => {
  return (
    <div>
      <button type="button" onClick={() => {}}>
        Good
      </button>
      <button type="button" onClick={() => {}}>
        Neutral
      </button>
      <button type="button" onClick={() => {}}>
        Bad
      </button>
    </div>
  );
};
export default Options;
